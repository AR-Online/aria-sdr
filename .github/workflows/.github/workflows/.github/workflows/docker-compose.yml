version: "3.9"
services:
  api:
    build: .
    image: aria-platform-api:latest
    ports:
      - "8000:8000"
    environment:
      FASTAPI_BEARER_TOKEN: "${FASTAPI_BEARER_TOKEN:-realizati}"
      RAG_ENABLE: "${RAG_ENABLE:-true}"
      # SUPABASE_URL: "https://<seu>.supabase.co"
      # SUPABASE_SERVICE_ROLE_KEY: "<service_role>"
      # OPENAI_API_KEY: "<openai>"
